import { render, screen } from '@testing-library/react';

it(`should display correct got character with image, first name, last name, 
    title, house and family.`,
  () => {
});

it("should fetch quotes and other content when clicking on quotes button", 
  () => {});

it("should display aliases", () => {});
it("should be possible to set character as favorite character", () => {});
it("should be possible to select favorite quote", () => {});